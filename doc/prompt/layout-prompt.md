请近乎完全复刻musicolet的功能，UI布局几乎跟musicolet一样：

	0、顶栏有这几个TAB：播放队列(即queues)、正在播放、文件夹、专辑列表、艺术家列表（再细分子tab:Artists/Album Artists/Composers）、风格、歌曲清单(即playlists)、搜索、汉堡菜单。

	1、队列TAB：要支持在顶部多队列下拉切换，点击下拉队列组件，弹出浮层显示多个队列，可以拖动以调整队列的上下位置，可以改队列名、删除队列。队列中的歌曲列表显示每首歌的标题、艺术家、专辑名、时长，歌曲列表上方还显示一排横向操作栏（三角形播放按钮[继续播放这个队列]、排序logo型按钮、当前播放歌曲位置编号/队列内总歌曲数、保存按钮[保存为播放清单“队列名”、加入到别的歌单]、省略号样式按钮（分享队列中所有歌曲、导出为m3u文件、进入多选模式））。每首歌右边有个省略标点样式的按钮，点击这个按钮，以居中纵向窄浮层显示菜单(歌曲信息、移出队列、插队待播、排到队尾、预览即听、此曲播完即停、编辑元数据、音频裁剪器、分享、永久删除)，在菜单浮层的右上角还有一个空心/实心的心形按钮。歌曲列表的底部有一行固定悬浮搜索框用于搜索本视图内的歌曲列表。在队列内歌曲列表启用多选歌曲模式下，底部会弹出宽扁的操作浮层，有三个按钮：选项（移出队列、立即播放、乱序播放、加入喜爱、移出喜爱、编辑元数据[仅当选择单曲]、分享、永久删除）、高级选择器（全选、反选、连续选择两首歌之间的多首）、取消（点击会推出选择模式）。

	2、正在播放TAB：上方显示封面、点击封面显示歌词。腰部靠下合适的位置显示歌曲的标题、艺术家、专辑名。往下一行显示一排按钮：心形按钮、圆形info按钮（点击后弹出纵向窄浮层显示封面、文件名、文件路径、歌曲标题、艺术家、专辑名、专辑作者、作曲人、风格、歌词编辑查看按钮、曲目编号、Disc编号、年份、备注、时长、比特率、采样率、采样位数、格式、编码、声道、文件大小、入库时间、修改时间、最近播放时间、播放数，浮层的左下角显示按钮"编辑元数据"，浮层的右下角显示按钮"完成"）、加入歌单按钮（点击后弹出纵向窄浮层，浮层的顶部显示“艺术家 - 歌曲标题”，往下一行显示并排两个大TAB"加入歌单"和"移出歌单"，往下接着就是每行列出一个歌单名的checkbox，直到所有歌单列完，底部悬浮一行显示"创建新歌单"[仅当选择"加入歌单"TAB时才显示]、取消、加入/移出）、播放速度（0.25/0.5/0.75/1.0/1.25/1.5/1.75/2.0）、均衡器、省略号样式按钮、随机播放logo/顺序播放logo/循环播放logo。往下一行显示播放进度条和时间点/总时长。往下一行显示：上一曲logo、快退logo、播放/暂停logo、快进logo、下一曲logo。

	3、文件夹TAB：第一行显示几乎一整行搜索栏，右边有一个排序按钮（可按文件夹名、歌曲数、时长、更新日期进行升序/降序），往下一行开始以纵向列出所有的文件夹列表，每行最左边显示folder-logo，剩下的右边再上下拆分成两行分别显示文件夹名、完整路径。点击某个文件夹即可列出歌曲列表，歌曲列表的样式和功能和队列中歌曲列表的相似，这里就不赘述。歌曲列表的底部有一行固定悬浮搜索框用于搜索本视图内的歌曲列表。在文件夹内歌曲列表启用多选歌曲模式下，底部会弹出宽扁的操作浮层，有三个按钮：选项（立即播放、乱序播放、插队待播、加入当前播放队列、加入队列、加入歌单、加入喜爱、移出喜爱、编辑元数据[仅当选择单曲]、分享、永久删除）、高级选择器（全选、反选、连续选择两首歌之间的多首）、取消（点击会推出选择模式）。

	4、专辑TAB：第一行显示几乎一整行搜索栏，右边有一个排序按钮（可按文件夹名、歌曲数、时长、更新日期进行升序/降序），往下一行开始以纵向列出所有的专辑列表，每行最左边显示Album封面，剩下的右边显示专辑名。点击某个专辑即可列出歌曲列表，歌曲列表的样式和功能和队列中歌曲列表的相似，这里就不赘述。歌曲列表的底部有一行固定悬浮搜索框用于搜索本视图内的歌曲列表。在专辑内歌曲列表启用多选歌曲模式下，底部会弹出宽扁的操作浮层，有三个按钮：选项（立即播放、乱序播放、插队待播、加入当前播放队列、加入队列、加入歌单、加入喜爱、移出喜爱、编辑元数据[仅当选择单曲]、分享、永久删除）、高级选择器（全选、反选、连续选择两首歌之间的多首）、取消（点击会推出选择模式）。

	5、艺术家TAB：第一行有三个子TAB(Artists/Album Artists/Composers)筛选。下一行显示几乎一整行搜索栏，右边有一个排序按钮（可按文件夹名、歌曲数、时长、更新日期进行升序/降序）。往下一行开始以纵向列出所有的艺术家列表，一行一个。点击某个艺术家，进入子页面内：第一行显示返回左箭头、艺术家名，往下一行显示类似"7个专辑"字样，往下一行以无限横向滚动模式展示该艺术家名下的专辑列表（第一个是全部专辑，后面才是具体每个专辑的封面和专辑名），往下一行显示类似“11首歌 合计时长53:36，往下一行以右对齐显示乱序播放logo、排序logo、省略号样式按钮（点击弹出浮层，有立即播放、插队待播、加入当前播放队列、加入队列、加入歌单、分享）。在艺术家内歌曲列表启用多选歌曲模式下，底部会弹出宽扁的操作浮层，有三个按钮：选项（立即播放、乱序播放、插队待播、加入当前播放队列、加入队列、加入歌单、加入喜爱、移出喜爱、编辑元数据[仅当选择单曲]、分享、永久删除）、高级选择器（全选、反选、连续选择两首歌之间的多首）、取消（点击会推出选择模式）。歌曲列表的底部有一行固定悬浮搜索框用于搜索本视图内的歌曲列表。歌曲列表的底部有一行固定悬浮搜索框用于搜索本视图内的歌曲列表。

	6、风格TAB：第一行显示几乎一整行搜索栏，右边有一个排序按钮（可按文件夹名、歌曲数、时长、更新日期进行升序/降序），往下一行开始以纵向列出所有的风格列表，每行显示风格名。点击某个风格即可列出歌曲列表，歌曲列表的样式和功能和队列中歌曲列表的相似，这里就不赘述。歌曲列表的底部有一行固定悬浮搜索框用于搜索本视图内的歌曲列表。在专辑内歌曲列表启用多选歌曲模式下，底部会弹出宽扁的操作浮层，有三个按钮：选项（立即播放、乱序播放、插队待播、加入当前播放队列、加入队列、加入歌单、加入喜爱、移出喜爱、编辑元数据[仅当选择单曲]、分享、永久删除）、高级选择器（全选、反选、连续选择两首歌之间的多首）、取消（点击会推出选择模式）。

	7、歌单TAB：从上到下每行列出播放列表，有系统保留歌单（全部歌曲、喜爱、最近加入、最近播放、最多播放、尚未播放），个人歌单（
	先另起一行靠左显示"个人歌单"文案，靠右显示按钮"+ 新建歌单"，接下来换行显示歌单1、歌单2、...）。最底部固定悬浮显示一个搜索栏"搜索歌单..."。 点击"全部歌曲"或"喜爱"歌单，显示出里面的歌曲列表，有歌曲数、合计播放时长，在歌曲列表跟队列中歌曲列表功能类似，列表的上方有三个按钮（随机播放logo、排序、三点省略号样式按钮），每首歌也有封面、歌曲标题、艺术家、专辑名、时长、三点省略号样式按钮，支持多选模式（此时底部会固定悬浮多选面板，这个面板在别的TAB中提过了，不赘述）。歌曲列表的底部有一行固定悬浮搜索框用于搜索本视图内的歌曲列表。

	8、搜索TAB：顶部搜索栏，不要点击按钮来搜索。下方一行列出一个搜索历史关键词。底部固定悬浮一个无边框按钮“清除搜索历史”

	9、汉堡菜单TAB：睡眠倒计时、语言、界面（下设主题、Tab-bar位置）、标签解析偏好（默认全部标签都以分隔符识别多作者[默认分隔符集合有" , ; | & ft. feat. "]， 还可以按演唱者、专辑作者、作曲人、风格分别设置分隔符）、备份/还原本系统数据（这里备份的数据不适用于musicolet app，只是本系统数据备份）、从Musicolet备份数据导入（这个功能会创建一个数据导入过程(procedure)）

	